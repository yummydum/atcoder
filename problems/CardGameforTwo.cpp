#include <bits/stdc++.h>
using namespace std;

int main()
{

    int alice = 0;
    int bob = 0;
    int N;
    cin >> N;
    int cards[N];

    for (int i = 0; i < N; i++)
        cin >> cards[i];

    sort(cards, cards + N, greater<int>());

    for (int i = 0; i < N; i++)
    {
    }
}
